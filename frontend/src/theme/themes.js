const SHARED_STATUS = {
  pending: '#F59E0B',
  assigned: '#3B82F6',
  progress: '#8B5CF6',
  paused: '#EC4899',
  completed: '#10B981',
  cancelled: '#6B7280',
  danger: '#EF4444',
  success: '#10B981',
  warning: '#F59E0B',
  info: '#3B82F6',
};

function hexToRgb(hex) {
  const normalized = hex.replace('#', '');
  const value = normalized.length === 3
    ? normalized.split('').map((char) => char + char).join('')
    : normalized;

  const parsed = Number.parseInt(value, 16);

  return {
    r: (parsed >> 16) & 255,
    g: (parsed >> 8) & 255,
    b: parsed & 255,
  };
}

function withAlpha(hex, alpha) {
  const { r, g, b } = hexToRgb(hex);
  return `rgba(${r}, ${g}, ${b}, ${alpha})`;
}

function createTheme({
  id,
  label,
  description,
  mode,
  bgPrimary,
  bgSecondary,
  bgTertiary,
  bgElevated,
  cardStart,
  cardEnd,
  accent,
  accentLight,
  accentContrast,
  textPrimary,
  textSecondary,
  textMuted,
  textInverse,
  borderBase,
  neutralBase,
  spotlight,
}) {
  const isDark = mode === 'dark';
  const shadowBase = isDark ? '0, 0, 0' : '15, 23, 42';

  return {
    id,
    label,
    description,
    mode,
    preview: [accent, accentLight, bgPrimary],
    variables: {
      '--page-backdrop': `radial-gradient(circle at top, ${withAlpha(spotlight, isDark ? 0.16 : 0.1)} 0%, transparent 32%), linear-gradient(180deg, ${bgPrimary} 0%, ${bgSecondary} 100%)`,
      '--bg-primary': bgPrimary,
      '--bg-secondary': bgSecondary,
      '--bg-tertiary': bgTertiary,
      '--bg-elevated': bgElevated,
      '--bg-hover': bgElevated,
      '--bg-card': `linear-gradient(145deg, ${cardStart} 0%, ${cardEnd} 100%)`,
      '--bg-card-solid': cardStart,
      '--bg-navbar': withAlpha(bgSecondary, isDark ? 0.82 : 0.9),
      '--bg-sidebar': bgSecondary,
      '--bg-modal': `linear-gradient(145deg, ${cardStart} 0%, ${cardEnd} 100%)`,
      '--bg-input': withAlpha(neutralBase, isDark ? 0.05 : 0.045),
      '--bg-input-focus': withAlpha(accent, isDark ? 0.08 : 0.06),
      '--bg-tab': withAlpha(neutralBase, isDark ? 0.035 : 0.05),
      '--bg-tab-active': withAlpha(neutralBase, isDark ? 0.09 : 0.11),
      '--bg-soft': withAlpha(neutralBase, isDark ? 0.04 : 0.045),
      '--bg-soft-strong': withAlpha(neutralBase, isDark ? 0.07 : 0.08),
      '--bg-soft-hover': withAlpha(neutralBase, isDark ? 0.11 : 0.13),
      '--bg-overlay': isDark ? 'rgba(2, 6, 23, 0.68)' : 'rgba(15, 23, 42, 0.22)',
      '--modal-overlay': isDark ? 'rgba(2, 6, 23, 0.74)' : 'rgba(15, 23, 42, 0.34)',
      '--text-primary': textPrimary,
      '--text-secondary': textSecondary,
      '--text-muted': textMuted,
      '--text-inverse': textInverse,
      '--border': withAlpha(borderBase, isDark ? 0.14 : 0.12),
      '--border-hover': withAlpha(borderBase, isDark ? 0.26 : 0.22),
      '--border-strong': withAlpha(borderBase, isDark ? 0.34 : 0.28),
      '--accent': accent,
      '--accent-light': accentLight,
      '--accent-contrast': accentContrast,
      '--accent-gradient': `linear-gradient(135deg, ${accent} 0%, ${accentLight} 100%)`,
      '--accent-soft': withAlpha(accent, isDark ? 0.14 : 0.11),
      '--accent-soft-hover': withAlpha(accent, isDark ? 0.18 : 0.15),
      '--accent-glow': withAlpha(accent, isDark ? 0.28 : 0.2),
      '--accent-border': withAlpha(accent, isDark ? 0.26 : 0.22),
      '--surface-line': `linear-gradient(90deg, transparent 0%, ${withAlpha(neutralBase, isDark ? 0.1 : 0.12)} 50%, transparent 100%)`,
      '--surface-radial': `radial-gradient(circle at top right, ${withAlpha(accentLight, isDark ? 0.12 : 0.1)}, transparent 60%)`,
      '--sidebar-spotlight': `radial-gradient(ellipse at 30% 0%, ${withAlpha(spotlight, isDark ? 0.14 : 0.12)} 0%, transparent 70%)`,
      '--login-left-glow': `radial-gradient(circle, ${withAlpha(accent, isDark ? 0.12 : 0.1)} 0%, transparent 65%)`,
      '--login-right-glow': `radial-gradient(circle, ${withAlpha(accentLight, isDark ? 0.1 : 0.08)} 0%, transparent 65%)`,
      '--login-panel-spotlight': `radial-gradient(ellipse at 50% 40%, ${withAlpha(accent, isDark ? 0.08 : 0.06)} 0%, transparent 60%)`,
      '--focus-ring': withAlpha(accent, isDark ? 0.24 : 0.18),
      '--scrollbar': withAlpha(neutralBase, isDark ? 0.12 : 0.18),
      '--scrollbar-hover': withAlpha(neutralBase, isDark ? 0.2 : 0.28),
      '--table-row-border': withAlpha(neutralBase, isDark ? 0.04 : 0.08),
      '--table-row-hover': withAlpha(neutralBase, isDark ? 0.03 : 0.055),
      '--nav-active-shadow': `inset 0 0 20px ${withAlpha(accent, isDark ? 0.08 : 0.06)}`,
      '--shadow-sm': `0 2px 10px rgba(${shadowBase}, ${isDark ? 0.28 : 0.12})`,
      '--shadow-md': `0 12px 28px rgba(${shadowBase}, ${isDark ? 0.3 : 0.14})`,
      '--shadow-lg': `0 20px 52px rgba(${shadowBase}, ${isDark ? 0.4 : 0.18})`,
      '--shadow-card': `0 10px 28px rgba(${shadowBase}, ${isDark ? 0.28 : 0.1}), inset 0 1px 0 ${withAlpha(neutralBase, isDark ? 0.04 : 0.18)}`,
      '--shadow-accent': `0 0 32px ${withAlpha(accent, isDark ? 0.18 : 0.12)}`,
      '--status-pending': SHARED_STATUS.pending,
      '--status-assigned': SHARED_STATUS.assigned,
      '--status-progress': SHARED_STATUS.progress,
      '--status-paused': SHARED_STATUS.paused,
      '--status-completed': SHARED_STATUS.completed,
      '--status-cancelled': SHARED_STATUS.cancelled,
      '--danger': SHARED_STATUS.danger,
      '--success': SHARED_STATUS.success,
      '--warning': SHARED_STATUS.warning,
      '--info': SHARED_STATUS.info,
    },
  };
}

export const DEFAULT_THEME_ID = 'minimal-light';
export const THEME_STORAGE_KEY = 'hotel-ops-theme';

export const themes = [
  createTheme({
    id: 'dark-blue',
    label: 'Blue Mist',
    description: 'Light airy blues with clean sapphire accents.',
    mode: 'light',
    bgPrimary: '#F4F8FF',
    bgSecondary: '#EAF1FF',
    bgTertiary: '#FFFFFF',
    bgElevated: '#DCE8FF',
    cardStart: '#FFFFFF',
    cardEnd: '#EEF4FF',
    accent: '#4F7DE8',
    accentLight: '#8EB4FF',
    accentContrast: '#F4F8FF',
    textPrimary: '#13233F',
    textSecondary: '#587095',
    textMuted: '#7A8EA9',
    textInverse: '#F8FBFF',
    borderBase: '#203A63',
    neutralBase: '#10213A',
    spotlight: '#7AA7FF',
  }),
  createTheme({
    id: 'emerald-green',
    label: 'Emerald Light',
    description: 'Fresh mint surfaces with a softer hospitality green.',
    mode: 'light',
    bgPrimary: '#F3FBF7',
    bgSecondary: '#E8F6EE',
    bgTertiary: '#FFFFFF',
    bgElevated: '#D9EEE2',
    cardStart: '#FFFFFF',
    cardEnd: '#EEF8F2',
    accent: '#28B56B',
    accentLight: '#7ED9A8',
    accentContrast: '#F2FFF7',
    textPrimary: '#163126',
    textSecondary: '#4F7462',
    textMuted: '#799382',
    textInverse: '#F7FFFA',
    borderBase: '#214737',
    neutralBase: '#163126',
    spotlight: '#70D49B',
  }),
  createTheme({
    id: 'purple-neon',
    label: 'Purple Neon',
    description: 'Midnight violets with a sharper futuristic accent.',
    mode: 'dark',
    bgPrimary: '#0D0A1B',
    bgSecondary: '#161029',
    bgTertiary: '#21153B',
    bgElevated: '#2A1E49',
    cardStart: '#22163E',
    cardEnd: '#161029',
    accent: '#B26BFF',
    accentLight: '#E48DFF',
    accentContrast: '#FAF0FF',
    textPrimary: '#FAF5FF',
    textSecondary: '#C8B4E2',
    textMuted: '#786A95',
    textInverse: '#100A1F',
    borderBase: '#F2DAFF',
    neutralBase: '#FFFFFF',
    spotlight: '#B26BFF',
  }),
createTheme({
    id: 'boutique-midnight',
    label: 'Boutique Midnight',
    description: 'Deep charcoal surfaces with elegant brushed gold accents.',
    mode: 'dark',
    bgPrimary: '#0A0A0A',
    bgSecondary: '#141414',
    bgTertiary: '#1A1A1A',
    bgElevated: '#262626',
    cardStart: '#1A1A1A',
    cardEnd: '#141414',
    accent: '#D4AF37',       // Classic Gold
    accentLight: '#F3E5AB',  // Soft Champagne
    accentContrast: '#0A0A0A',
    textPrimary: '#F5F5F5',
    textSecondary: '#A3A3A3',
    textMuted: '#737373',
    textInverse: '#0A0A0A',
    borderBase: '#E5E5E5',
    neutralBase: '#FFFFFF',
    spotlight: '#D4AF37',
  }),
  createTheme({
    id: 'minimal-light',
    label: 'Minimal Light',
    description: 'Bright neutral surfaces with refined slate-blue accents.',
    mode: 'light',
    bgPrimary: '#F5F7FB',
    bgSecondary: '#EDF1F7',
    bgTertiary: '#FFFFFF',
    bgElevated: '#E5EBF4',
    cardStart: '#FFFFFF',
    cardEnd: '#F1F5FB',
    accent: '#2563EB',
    accentLight: '#60A5FA',
    accentContrast: '#EFF6FF',
    textPrimary: '#102033',
    textSecondary: '#4B5E75',
    textMuted: '#718198',
    textInverse: '#F8FBFF',
    borderBase: '#1E293B',
    neutralBase: '#0F172A',
    spotlight: '#60A5FA',
  }),
  createTheme({
    id: 'chatgpt-grey',
    label: 'Grey',
    description: 'Soft graphite neutrals inspired by ChatGPT-style interfaces.',
    mode: 'light',
    bgPrimary: '#F4F4F4',
    bgSecondary: '#ECECEC',
    bgTertiary: '#FFFFFF',
    bgElevated: '#E1E1E1',
    cardStart: '#FFFFFF',
    cardEnd: '#F1F1F1',
    accent: '#4B5563',
    accentLight: '#9CA3AF',
    accentContrast: '#F9FAFB',
    textPrimary: '#202123',
    textSecondary: '#565869',
    textMuted: '#8A8C99',
    textInverse: '#F8F9FB',
    borderBase: '#202123',
    neutralBase: '#202123',
    spotlight: '#B6BCC6',
  }),

  createTheme({
    id: 'oled-accessibility',
    label: 'High Contrast (OLED)',
    description: 'Pure black for battery saving with high-visibility cyan accents.',
    mode: 'dark',
    bgPrimary: '#000000',    // Pure Black (turns off OLED pixels)
    bgSecondary: '#000000',
    bgTertiary: '#111111',
    bgElevated: '#1A1A1A',
    cardStart: '#111111',
    cardEnd: '#000000',
    accent: '#06B6D4',       // High-vis Cyan
    accentLight: '#67E8F9',
    accentContrast: '#000000',
    textPrimary: '#FFFFFF',  // Pure White
    textSecondary: '#D4D4D8',
    textMuted: '#A1A1AA',
    textInverse: '#000000',
    borderBase: '#FFFFFF',   // Stronger borders for visibility
    neutralBase: '#FFFFFF',
    spotlight: '#06B6D4',
  }),
];

export const themesById = themes.reduce((collection, theme) => {
  collection[theme.id] = theme;
  return collection;
}, {});
